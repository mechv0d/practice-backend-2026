# Survey API

## Описание

API-сервис для создания, управления и анализа опросов и голосований. Поддерживает роли автора и респондента, разные типы вопросов, жизненный цикл опросов и базовую аналитику. Разработан на Laravel с Eloquent ORM и MySQL.

## Автор

Рем Сергей, группа 1ИСП-21

## Требования

- PHP
- Composer
- MySQL
- Laravel

## Установка

1. Клонируйте репозиторий: `git clone https://github.com/mechv0d/practice-backend-2026.git`
2. Установите зависимости: `composer install`
3. Скопируйте .env.example в .env и настройте его для MySQL.
4. Сгенерируйте ключ: `php artisan key:generate`
5. Запустите миграции: `php artisan migrate`
6. Запустите сервер: `php artisan serve`

## Эндпоинты

Эндпоинты спроектированы как RESTful, с использованием префикса /api для ясности. Аутентификация требуется для всех, кроме публичных (например, прохождения опросов). Методы и описания:

### Пользователи и Аутентификация

- **POST /api/register** — Регистрация пользователя (тело: name, email, password). Возвращает токен.
- **POST /api/login** — Авторизация (тело: email, password). Возвращает токен.
- **GET /api/user** — Получение профиля текущего пользователя (аутентифицировано).

### Управление Опросами

- **POST /api/surveys** — Создание опроса (тело: title, description). Статус: draft. Аутентифицировано (автор).
- **GET /api/surveys** — Список опросов текущего автора.
- **GET /api/surveys/{id}** — Получение деталей опроса (включая вопросы и варианты).
- **PUT /api/surveys/{id}** — Обновление опроса (тело: title, description). Только если draft.
- **POST /api/surveys/{id}/publish** — Публикация опроса.
- **POST /api/surveys/{id}/close** — Закрытие опроса.

### Управление Вопросами

- **POST /api/surveys/{id}/questions** — Добавление вопроса (тело: text, type, order, options[] для choice).
- **PUT /api/questions/{id}** — Обновление вопроса (тело: text, order, options[]). Только если опрос draft.
- **DELETE /api/questions/{id}** — Удаление вопроса. Только если draft.

### Управление Вариантами

- **POST /api/questions/{id}/options** — Добавление варианта (тело: text).
- **PUT /api/options/{id}** — Обновление варианта (тело: text).
- **DELETE /api/options/{id}** — Удаление варианта.

### Прохождение Опросов

- **GET /api/surveys/{id}/form** — Получение формы опроса для прохождения (вопросы и варианты). Доступно если published.
- **POST /api/surveys/{id}/responses** — Отправка ответов (тело: answers[] с валидацией по типу). Аутентифицировано (респондент). Только один раз.

### Аналитика

- **GET /api/surveys/{id}/results** — Получение результатов (количество респондентов, статистика по вопросам).
- **GET /api/surveys/{id}/results/export** — Экспорт результатов в JSON.

Все эндпоинты возвращают JSON с HTTP-статусами (200 OK, 201 Created, 400 Bad Request и т.д.). Валидация и ошибки обрабатываются в контроллерах.

## Тестирование

Используйте Postman для тестирования эндпоинтов. Аутентификация через Bearer Token после /api/login.

## Дополнительно

- ER-диаграмма: См. /docs/er-diagram.png